#include <stdio.h>

int main() {
    double num_a, num_b;
    scanf("%lf %lf", &num_a, &num_b);
    
    if (num_a > num_b){
        printf(">");
    } else if (num_a == num_b){
        printf("==");
    } else {
        printf("<");
    }
}
